# 市场情报系统：先看这里

> **新版推荐入口：双击根目录 `一键部署.cmd`。** 只需先装好并启动 Docker Desktop；
> 脚本会自动生成首次配置。新版网页地址为 http://127.0.0.1:8080/ ，浏览器说明为
> http://127.0.0.1:8080/help 。下面保留旧版手动部署速查，首次使用请以 `使用说明.txt` 为准。

> 新下载的代码不能直接运行。第一次使用请先阅读根目录的 `使用说明.txt`，完成
> Docker Desktop、WSL 2、`.env` 和密码配置，再运行环境检查脚本。

首次安装命令：

```powershell
Copy-Item .env.example .env
notepad .env
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\检查运行环境.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\finish-docker-n8n.ps1
```

以下内容用于系统已经完成首次安装后的日常操作。

## 第一步：确认系统正在运行

打开 PowerShell：

```powershell
cd path\to\market-intelligence-system
docker compose ps
```

看到 `postgres`、`api`、`collector`、`analysis`、`n8n` 五个服务为 `Up` 即正常。

如果没有运行：

```powershell
docker compose up -d
```

也可以直接双击 `scripts\启动Docker系统.cmd`。

## 第二步：打开三个常用页面

- 最新市场情报报告：<http://127.0.0.1:8000/reports/latest>
- API 操作页面：<http://127.0.0.1:8000/api/docs>
- n8n 自动化页面：<http://127.0.0.1:5678>

第一次打开 n8n 时，需要由你创建本机管理员账号。工作流已经导入并发布，不需要再次导入。

## 第三步：手动生成新报告

方法一：在 n8n 打开“市场情报自动收集与报告”，点击 Execute workflow。

方法二：在 API 页面找到 `POST /api/runs`，点击 `Try it out`，再点击 `Execute`。

系统会依次完成：

```text
n8n → API → Crawl4AI/Browser Use → PostgreSQL 去重 → Pandas 分析 → HTML 报告
```

## 第四步：添加关注的网站

在 API 页面调用 `POST /api/sources`：

```json
{
  "name": "某公司新闻中心",
  "url": "https://example.com/news",
  "kind": "static",
  "enabled": true,
  "tags": ["竞争对手", "产品动态"]
}
```

- 普通公开网页使用 `static`，由 Crawl4AI 采集。
- 必须点击或翻页的网页使用 `interactive`，由 Browser Use 操作。
- 不采集需要登录、验证码、付费或禁止自动访问的内容。

## 第五步：用 DeepSeek 分析已采集数据

在 API 页面调用 `POST /api/analysis/ask`：

```json
{
  "question": "概括三家公司值得关注的市场信号，并说明信息不足之处。",
  "item_limit": 100
}
```

DeepSeek Key 已从本机 `.env` 读取。不要把 Key 发到聊天、截图或提交到 Git。

## 第六步：停止和重新启动

停止但保留全部数据：

```powershell
docker compose stop
```

也可以直接双击 `scripts\停止Docker系统.cmd`。

重新启动：

```powershell
docker compose start
```

不要运行 `docker compose down -v`，因为 `-v` 会删除数据库和持久化数据。

## 深入学习

- `docs\Docker-n8n-完整操作步骤.md`：从启动、使用到排错的完整教程。
- `docs\一步一步理解系统.md`：从信源进入到报告生成的代码和数据流。
- `docs\验收记录.md`：本机实际通过了哪些功能。
- `README.md`：完整架构、开发运行和云服务器部署说明。
